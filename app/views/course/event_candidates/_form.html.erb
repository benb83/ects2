<% if false %>
<%= form_for(@course_event_candidate) do |f| %>
  <% if @course_event_candidate.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@course_event_candidate.errors.count, "error") %> prohibited this course_event_candidate from being saved:</h2>

      <ul>
      <% @course_event_candidate.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :course_event_id %><br>
    <%= f.number_field :course_event_id %>
  </div>
  <div class="field">
    <%= f.label :person_candidate_id %><br>
    <%= f.number_field :person_candidate_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
<% end %>